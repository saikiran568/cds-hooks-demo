{
  "name": "cerner-cds-hooks",
  "version": "1.0.0",
  "description": "CDS Hooks service (patient-view) for use with a SMART on FHIR app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}